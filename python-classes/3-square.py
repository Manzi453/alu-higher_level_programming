#!/usr/bin/python3
"""Define a square"""


class Square:
    """"Creates a square.
    private instance attrributes: size 
    """
    def __init__(self, size=0):
        """initializes the data"""
        if not isinstance(size,)
        raise TypeError("size must be an integer"
                elif size < 0:
                raise ValueError("size V I must be >= 0")
                self.__size = size
                
                def area(self)
                """Return square area"""
                return self.__size ** 2
